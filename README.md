# Notas:
Este es mi primer programa en Node
```Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Toma la base de la tabla de multiplicar     [number] [required]
  -l, --listar   Si es True permite Listar caso contrario solo escribe el
                 archivo con el nombre de la Tabla
                                           [boolean] [required] [default: false]
  -m, --limit    Limite de veces para la tabla               [number] [required]
```